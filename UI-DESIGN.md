# 文言 · 上海 UI 方案

参考：https://www.8bitcn.com/ 。采用其黑白高对比、像素阶梯按钮、方格状态与分段进度条的视觉语言，保留中文学习场景的阅读习惯。

## 页面结构

- 今日训练：日期与样例提示 → 今日学习目标 → 训练任务与开始按钮 → 能力线索 → 最近记录。桌面右侧呈现像素书本、连续学习与学习方法；手机单列排列，省略装饰书本。
- 答题：桌面左侧原文、右侧问题；手机原文在上、问题在下。选择、提交、参考解析、自评、下一题仍按原有顺序进行。
- 学习记录：累计样本与逐题轨迹两栏；手机改为单列。
- 完成页与说明弹窗：使用同一套方框、按钮与字体规范。

## 视觉规范

- 主色：墨黑 `#171717`、纸白 `#ffffff`；强调色：淡黄绿 `#e9ee9b`；辅助文字最低使用 `#626262`。
- 面板：2–3 px 实线边框，主要面板配 5 px 硬阴影；主操作使用阶梯边缘。
- 字体：中文界面采用系统字体，原文与参考答案使用宋体类衬线字体；数字与英文标签采用本地等宽字体。无需加载外部字体。
- 进度：保留原生 progress 语义，视觉改为横向像素条；连续学习使用七日方格，避免凭空添加等级、积分或能力值。
- 操作：主要按钮至少 48 px 高；保留键盘焦点与减少动态效果设置；选中状态同时改变边框和底色。

## 实现与验证

`pixel-theme.css` 在基础样式之后加载，集中管理此次视觉改造；`index.html` 增加主题引用及装饰性 SVG。`app.js` 修正完成页进度条停留在 75% 的显示问题。原有学习引擎与本地数据结构保持兼容。

已用独立浏览器测试桌面 1440 px、手机 390 px 与 320 px 布局；检查答题、参考答案、自评、下一题、暂停、完整训练总结、记录与说明弹窗。手机表格列宽自适应，页面无横向溢出。测试记录写入独立 localhost 来源，不触及原 file 页面中的学习记录。

此次为 UI 改造，题库审核与学习算法仍遵循现有样例版边界。
